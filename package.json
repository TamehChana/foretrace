{
  "name": "foretrace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "vercel-build": "npm run build -w @foretrace/shared && npm run build -w @foretrace/web",
    "stage-vercel-dist": "node scripts/stage-vercel-dist.mjs"
  },
  "dependencies": {
    "vite": "^8.0.10"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20"
  }
}
