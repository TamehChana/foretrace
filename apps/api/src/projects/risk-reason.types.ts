/** Machine-readable `code` from `ProjectRiskService` (non-exhaustive). */
export type RiskReasonRow = { code: string; detail: string };

/** Actionable PM recommendation derived from rule reasons (evidence-linked). */
export type RiskRecommendationRow = { code: string; detail: string };
