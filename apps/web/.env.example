# Optional for local prod-style testing: Nest API URL, no trailing slash.
# Leave unset in dev so `/health`, `/auth`, and `/organizations` use the Vite proxy to localhost:3000.
#
# Production (Vercel): Project → Settings → Environment Variables
# VITE_API_URL=https://foretrace-api.onrender.com

# VITE_API_URL=http://localhost:3000
