{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci && npm install vite@8.0.11 --no-save --workspace=@foretrace/web",
  "buildCommand": "npm run build -w @foretrace/shared && npm run build -w @foretrace/web && if [ -d apps/web/dist ]; then rm -rf dist && cp -r apps/web/dist dist; elif [ -f dist/index.html ]; then :; else ls -la; ls -la apps/web 2>/dev/null || true; exit 1; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
