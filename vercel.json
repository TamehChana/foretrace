{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci && npm install vite@8.0.11 --no-save --workspace=@foretrace/web",
  "buildCommand": "npm run build -w @foretrace/shared && npm run build -w @foretrace/web && cd \"$(git rev-parse --show-toplevel)\" && node scripts/stage-vercel-dist.mjs",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
